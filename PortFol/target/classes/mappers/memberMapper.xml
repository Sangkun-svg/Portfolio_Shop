<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="memberMapper">
	<insert id="register">
		INSERT INTO member (userId , userPass , userName , userPhone) 
			VALUES (#{userId},#{userPass},#{userName},#{userPhone})
	</insert>	
	
	<select id="signin" resultType="com.sh.Dto.UserDto">
		select 
			userId , userPass , userName , userPhone , userAddr1 , userAddr2 , userAddr3 , regDate , verify
		from 
			member
		where userId = #{userId}
	</select>
</mapper>

