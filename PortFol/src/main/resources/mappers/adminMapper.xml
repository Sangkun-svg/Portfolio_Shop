<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="adminMapper">
	<select id="userList" resultType="com.sh.Dto.UserDto">
		SELECT
			userId As userId ,
			userName As userName ,
			userPhone As userPhone ,
			regDate As regDate ,
			Verify As Verify
		FROM
			member
	</select>

	<insert id="proRegister">
		insert 
			into product (proCode , proName , proPrice , proStock , proDescription , regDate , proImg)
		values 
			(#{proCode} , #{proName} , #{proPrice} , #{proStock} , #{proDescription} , #{regDate}, #{proImg})
	</insert>

	<select id="proList" resultType="com.sh.Dto.Product">
		SELECT
		bno As bno,
		proCode As
		proCode,
		proName As proName,
		proPrice As proPrice,
		proStock As proStock,
		proDescription As proDescription,
		proImg As proImg
		FROM
		product
	</select>

	<select id="proView" parameterType="int" resultType="com.sh.Dto.Product">
		SELECT
		bno , proCode, proName, proPrice, proStock,
		proDescription
		FROM
		product
		where
		bno = #{bno}
	</select>

	<update id="proUpdate" parameterType="com.sh.Dto.Product">
		update product set
		proCode =#{proCode},
		proName =#{proName},
		proPrice =#{proPrice},
		proDescription =#{proDescription}
		where bno =#{bno}
	</update>

	<delete id="proDelete" parameterType="int">
		delete from product where
		bno = #{bno}
	</delete>
</mapper>

